name: Daily NHL Predictions

on:
  schedule:
    # Runs at 11:00 AM UTC = 7:00 AM ET (before the day's games)
    - cron: "0 11 * * *"
  workflow_dispatch:  # Allow manual triggering from GitHub UI

jobs:
  predict:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install pandas requests scikit-learn

      - name: Run daily predictions
        env:
          GITHUB_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python predx_api_V1.py \
            --write_daily \
            --publish_github \
            --process_yesterday \
            --season_start 2025-10-07

      - name: Commit updated CSVs and JSON
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Copy Tidbyt files to docs/ for GitHub Pages serving
          # Raw URLs:
          #   https://raw.githubusercontent.com/stat-trick-hockey/API_Model/main/docs/prediction_rates.csv
          #   https://raw.githubusercontent.com/stat-trick-hockey/API_Model/main/docs/prediction_rates.json
          #   https://raw.githubusercontent.com/stat-trick-hockey/API_Model/main/docs/todays_picks.csv
          #   https://raw.githubusercontent.com/stat-trick-hockey/API_Model/main/docs/todays_picks.json
          cp output/prediction_rates.csv  docs/prediction_rates.csv
          cp output/prediction_rates.json docs/prediction_rates.json
          cp output/todays_picks.csv      docs/todays_picks.csv
          cp output/todays_picks.json     docs/todays_picks.json

          git add \
            output/history_predictions.csv \
            output/model_coefficients.csv \
            output/prediction_rates.csv \
            output/prediction_rates.json \
            output/todays_picks.csv \
            output/todays_picks.json \
            docs/prediction_rates.csv \
            docs/prediction_rates.json \
            docs/todays_picks.csv \
            docs/todays_picks.json

          git diff --cached --quiet || git commit -m "chore: update predictions $(date -u +%Y-%m-%d)"
          git pull --rebase
          git push
